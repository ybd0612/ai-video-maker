- 采用 `@/` 路径别名映射到 `src/` 目录，所有内部模块导入均使用绝对路径而非相对路径。
- 服务层函数（如 `runPipeline`）统一接收包含 `AbortSignal` 的配置对象，以支持外部触发的异步任务取消。
- 状态更新遵循不可变原则，在 `projectStore` 中使用 `updateActive` 辅助函数确保只更新当前激活项目的特定字段。
- 类型定义集中在 `src/lib/models.ts` 或各模块的 `types.ts` 中，并在组件中通过 `import type` 进行纯类型导入以符合 `verbatimModuleSyntax` 约束。